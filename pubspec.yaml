name: Salintinig
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  cupertino_icons: ^1.0.8
  http: ^1.5.0
  audioplayers: ^6.5.1
  google_fonts: ^6.3.2
  google_mlkit_language_id: ^0.13.0
  google_mlkit_translation: ^0.13.0
  permission_handler: ^12.0.1
  whisper_flutter_new: ^1.0.1
  camera: ^0.11.0
  camera_web: ^0.3.2
  google_mlkit_text_recognition: ^0.15.1
  record: ^5.2.0
  path_provider: ^2.1.2
  speech_to_text: ^7.3.0
  image_picker: ^1.0.0
  # ... changes: added flutter_blue_plus for BLE detection and connection
  flutter_blue_plus: ^1.31.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

dependency_overrides:
  record_linux: ^1.1.0
  record_platform_interface: ^1.5.0
